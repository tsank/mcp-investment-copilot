# servers/risk_engine/requirements.txt
# Complete and self-contained — no shared base file (Option A)
#
# Install:
#   cd servers/risk_engine
#   python -m pip install -r requirements.txt
#
# Transitive dependencies installed automatically:
#   statsmodels  — pulled in by arch for MLE fitting internals
#   patsy        — pulled in by statsmodels for formula handling

# MCP server SDK
mcp==1.3.0

# Data handling
numpy==1.26.4
pandas==2.2.2

# ARMA-GARCH fitting — Kevin Sheppard's arch library
# Used by compute_garch_forecast tool
# Requires statsmodels as transitive dependency
arch==6.3.0

# Statistical functions
# Used by compute_risk_metrics (percentile) and compute_garch_forecast
scipy==1.13.0

# Configuration and validation
python-dotenv==1.0.1
pydantic==2.7.4

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
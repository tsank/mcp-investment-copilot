# servers/compliance/requirements.txt
# Complete and self-contained — no shared base file (Option A)
#
# Install:
#   cd servers/compliance
#   python -m pip install -r requirements.txt
#
# Notable dependency:
#   pyyaml — loads YAML ruleset files at server startup
#             rules are stored in servers/compliance/rules/*.yaml
#             YAML separates rule configuration from rule checking logic

# MCP server SDK
mcp==1.3.0

# YAML ruleset loading
pyyaml==6.0.1

# Configuration and validation
python-dotenv==1.0.1
pydantic==2.7.4

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
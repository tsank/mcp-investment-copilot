# servers/market_data/requirements.txt
# Complete and self-contained — no shared base file (Option A)
#
# Install:
#   cd servers/market_data
#   python -m pip install -r requirements.txt

# MCP server SDK
mcp==1.3.0

# Data handling
numpy==1.26.4
pandas==2.2.2

# Live data source — present in v1 but only active when
# MARKET_DATA_SOURCE=live (v2). In v1 all reads are from CSV fixtures.
yfinance==0.2.40

# Configuration and validation
python-dotenv==1.0.1
pydantic==2.7.4

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7